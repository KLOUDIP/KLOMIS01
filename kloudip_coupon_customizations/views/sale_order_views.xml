<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="sale_order_view_form" model="ir.ui.view">
        <field name="name">sale.order.view.form.inherit.kloudip_coupon_customizations</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale_coupon.sale_order_view_form"/>
        <field name="arch" type="xml">

            <!--change action in sale coupon apply code wizard view button-->
            <xpath expr="//button[@name='%(sale_coupon.sale_coupon_apply_code_action)d']" position="attributes">
                <attribute name="name">sale_coupon_apply_code_action</attribute>
                <attribute name="type">object</attribute>
            </xpath>

            <!--replacing context we had a compute field named visible refunded amount-->
            <!--<button name="%(sale.action_view_sale_advance_payment_inv)d" position="attributes">-->
            <!--    <attribute name="context">{'default_visible_refunded_amount': visible_refunded_amount}</attribute>-->
            <!--</button>-->

            <!--Replacing context for the button cause some errors so we open wizard from python action-->
            <button name="%(sale.action_view_sale_advance_payment_inv)d" position="attributes">
                <attribute name="name">action_open_sale_make_invoice_advance_wizard</attribute>
                <attribute name="type">object</attribute>
            </button>

        </field>
    </record>

</odoo>